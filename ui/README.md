# Demo UI

This folder contains the optional demo dashboard for the assignment.

## Run

1. Generate artifacts first:

```bash
npm run crawl
npm run export:data
```

2. Start local UI server:

```bash
npm run ui:start
```

3. Open:

`http://localhost:4173`
